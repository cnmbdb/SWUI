# SWUI

一个极简的 React 交互原型：上下文动作拆分与 `voice-glow` 声音响应光束。

## 本地运行

```bash
npm install
npm run dev
```

麦克风需要在 `https` 或 `localhost` 下使用，并且必须由用户点击 `Listen` 后授权。

## 构建

```bash
npm run build
npm run preview
```

GitHub Pages 会由 Actions 构建 `dist/` 并部署到：

https://cnmbdb.github.io/SWUI/
